How to generate inputs:
Adjust values of global variables INSIDE and CHILDREN within grapher.py and run it.
To get algorithm parameters, set same variables in rowdy.py and run it.
k = (INSIDE+1)*(CHILDREN)
s = INSIDE + 1